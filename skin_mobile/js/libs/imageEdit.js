define(function(require, exports, modules){
	return function(jquery){
		(function($){
			var htmlText = '<div class="imgControl">'
								+'<button id="roll">旋转</button>'
								+'<canvas id="canvas"></canvas>'
							+'</div>';
			$.fn.imageEdit = function() {
			    
			    return this.waterFall;
			}
		})(jquery)
	}
})